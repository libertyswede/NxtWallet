<Page
    x:Class="NxtWallet.Views.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NxtWallet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:NxtWallet.Model"
    mc:Ignorable="d"
    Loading="OverviewPage_OnLoading"
    >

    <Page.Resources>
        <local:NxtAmountConverter x:Key="NxtAmountConverter" />
        <local:DateTimeConverter x:Key="DateTimeConverter" />
    </Page.Resources>

    <Grid Background="{StaticResource NxtGradientBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="56"/>
            <ColumnDefinition Width="16*"/>
            <ColumnDefinition Width="140"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" >
            <TextBlock TextWrapping="NoWrap" Text="{x:Bind ViewModel.NxtAddress}" FontFamily="Tw Cen MT" Foreground="White" FontSize="29.333" />
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <TextBlock TextWrapping="NoWrap" Text="Balance:" FontFamily="Tw Cen MT" Foreground="White" FontSize="24" />
                <TextBlock TextWrapping="NoWrap" Text="{x:Bind ViewModel.Balance}" FontFamily="Tw Cen MT" Foreground="White" FontSize="24" Margin="20,0,0,0" />
            </StackPanel>
            <Grid Margin="12,20,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="330" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Timestamp" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                <TextBlock Grid.Column="1" Text="Amount" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                <TextBlock Grid.Column="2" Text="Account" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                <TextBlock Grid.Column="3" Text="Message" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
            </Grid>
            <ListView ItemsSource="{x:Bind ViewModel.Transactions}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:Transaction">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="330" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind Timestamp, Converter={StaticResource DateTimeConverter}}" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                            <TextBlock Grid.Column="1" Text="{x:Bind NqtAmount, Converter={StaticResource NxtAmountConverter}}" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Account}" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                            <TextBlock Grid.Column="3" Text="{x:Bind Message}" FontFamily="Tw Cen MT" Foreground="White" FontSize="21.333" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal">
            <Ellipse Stroke="Black" Width="20" Height="20" HorizontalAlignment="Left" VerticalAlignment="Center" Fill="#FF00D100"/>
            <TextBlock Text="Online" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Margin="10,0,0,0" FontSize="21.333" />
        </StackPanel>
    </Grid>
</Page>
